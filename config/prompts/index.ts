/**
 * Stable prompt config entry for all model-facing text and prompt-only labels.
 * Keep prompt copy here so lib/llm stays focused on assembly and execution.
 */
export * from "./generation";
export * from "./few-shots";
