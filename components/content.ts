export {
  exampleTextsByTarget,
  expressionStyles as styles,
  resultCards as results,
  scenes,
  targetOptionsByScene,
} from "@/config";
export type { IconKind, SceneKind, TargetKind } from "@/config";
